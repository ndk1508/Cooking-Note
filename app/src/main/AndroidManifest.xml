<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===================== KHAI BÁO QUYỀN ===================== -->

    <!--
      Quyền đọc bộ nhớ ngoài, chỉ dành cho Android 12L (API 32) trở xuống.
      Thuộc tính "maxSdkVersion" đảm bảo quyền này không được yêu cầu trên Android 13+.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
      Quyền đọc hình ảnh, dành cho Android 13 (API 33) trở lên.
      Đây là quyền bắt buộc để thay thế cho READ_EXTERNAL_STORAGE khi truy cập ảnh.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <!--
      (TÙY CHỌN) Nếu sau này bạn muốn thêm chức năng chụp ảnh.
      Bạn cũng nên khai báo rằng ứng dụng của bạn sử dụng tính năng camera.
    -->
    <!-- <uses-feature android:name="android.hardware.camera" android:required="false" /> -->
    <!-- <uses-permission android:name="android.permission.CAMERA" /> -->


    <!-- ===================== CẤU HÌNH ỨNG DỤNG ===================== -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CookingNote">

        <!-- ✅ Màn hình chính (Màn hình khởi chạy) -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 Màn hình thêm món ăn -->
        <activity
            android:name=".activity.ThemMonAnActivity"
            android:exported="false" />
        <!-- exported="false" là một cài đặt bảo mật tốt, giữ nguyên nó. -->

        <!-- ⭐ MÀN HÌNH MỚI ĐÃ ĐƯỢC THÊM VÀO ĐÂY ⭐ -->
        <!-- 📖 Màn hình chi tiết món ăn -->
        <activity
            android:name=".activity.ChiTietMonAnActivity"
            android:exported="false" />


    </application>

</manifest>